import { RepositorySearchDirective } from './repositorySearch.directive';

export default angular
  .module('app.components.repositorySearch', [])
  .directive('repositorySearch', RepositorySearchDirective);
